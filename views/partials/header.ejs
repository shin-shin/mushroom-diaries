<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">

    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <link href="/stylesheets/materialize.css" type="text/css" rel="stylesheet" media="screen,projection" />
    <link href="/stylesheets/style.css" type="text/css" rel="stylesheet" media="screen,projection" />

    <title><%= title %></title>
</head>

<body class="blue-grey lighten-5">
    <nav class="blue-grey lighten-1" role="navigation">
        <div class="nav-wrapper container">
            <a id="logo-container" href="/" class="brand-logo">Mushroom&nbsp;Diaries</a>
            <ul class="right hide-on-med-and-down">
                <% if (user) { %>
                <li><a href="/">Current</a></li>
                <li><a href="/cards/new">New</a></li>
                <li><a href="/?archive">Archive</a></li>
                <!-- <li><a href="/">varieties</a></li>
                                <li><a href="/weather">weather</a></li>
                                <li><a href="/calendar">calendar</a></li> -->
                <% } %>
                <li>
                    <% if (user) { %>
                    <a href="/logout">Log Out</a>
                    <% } else { %>
                    <a href="/auth/google">Login with Google</a>
                    <% } %>
                </li>
            </ul>

            <ul id="nav-mobile" class="sidenav blue-grey white-link lighten-1">
                <% if (user) { %>
                <li><a href="/">Current</a></li>
                <li><a href="/cards/new">New</a></li>
                <li><a href="/?archive">Archive</a></li>
                <% } %>
                <li>
                    <% if (user) { %>
                    <a href="/logout">Log Out</a>
                    <% } else { %>
                    <a href="/auth/google">Login with Google</a>
                    <% } %>
                </li>
            </ul>
            <a href="#" data-target="nav-mobile" class="sidenav-trigger"><i class="material-icons">menu</i></a>


        </div>
        <div class="nav-wrapper container">
            <h6 class="left blue-grey-text text-lighten-2"><%= moment().format('MMM D') %></h6>
            <h6 class="right blue-grey-text text-lighten-2"><%= temp + String.fromCharCode(176) + "F"%></h6>
        </div>
    </nav>